{
  "name": "snaphomz-india-infra",
  "version": "1.0.0",
  "description": "AWS CDK infrastructure for snaphomz.in static site",
  "main": "app.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "cdk:synth": "cdk synth",
    "cdk:deploy": "cdk deploy",
    "cdk:diff": "cdk diff",
    "deploy": "npm run build && cdk deploy --require-approval never",
    "deploy:demo": "npm run build && cdk deploy --require-approval never",
    "destroy": "cdk destroy"
  },
  "keywords": [],
  "author": "Snaphomz Inc",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/jest": "^29.5.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "aws-cdk": "^2.100.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.100.0",
    "constructs": "^10.0.0"
  }
}
